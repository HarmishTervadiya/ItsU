{
  "name": "itsu-monorepo",
  "private": true,
  "packageManager": "bun@1.3.6",
  "engines": {
    "node": ">=18",
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "mobile",
    "backend",
    "anchor",
    "shared"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.9.3"
  }
}
